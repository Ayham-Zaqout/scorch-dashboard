"use client";
import { useState } from "react";
import { Building2, Clock, DollarSign, Globe, Mail, MapPin, Percent, Phone, Save, Store } from "lucide-react";
const defaults = { deliveryFee: 3.99, taxRate: 8.5, preparationTime: 20, minOrder: 12, currency: "USD", restaurantName: "Scorch Kitchen", restaurantEmail: "hello@scorch.com", restaurantPhone: "+1 (415) 555-0100", restaurantWebsite: "www.scorch.com", restaurantAddress: "248 Market St, San Francisco, CA 94103" };
export default function SettingsPage() { const [form, setForm] = useState(defaults), [saving, setSaving] = useState(false), [saved, setSaved] = useState(false); const update = (key, value) => setForm((f) => ({ ...f, [key]: value })); const save = () => { setSaving(true); setTimeout(() => { setSaving(false); setSaved(true); setTimeout(() => setSaved(false), 2500); }, 500); }; return <div className="space-y-7"><header className="flex flex-wrap items-start justify-between gap-4"><div><h1 className="text-2xl font-bold tracking-tight text-ink-900">Settings</h1><p className="mt-2 text-sm text-ink-500">Configure your restaurant operations and preferences.</p></div><button onClick={save} disabled={saving} className="inline-flex h-10 items-center gap-2 rounded-lg bg-brand-500 px-4 text-sm font-semibold text-white hover:bg-brand-600 disabled:opacity-60"><Save size={17} />{saving ? "Saving..." : "Save changes"}</button></header><div className="grid gap-5 lg:grid-cols-2"><Card title="Operations" sub="Pricing and fulfillment settings" icon={DollarSign}><div className="grid gap-4 sm:grid-cols-2"><Field icon={DollarSign} label="Delivery fee ($)" type="number" value={form.deliveryFee} onChange={(v) => update("deliveryFee", Number(v))} /><Field icon={Percent} label="Tax rate (%)" type="number" value={form.taxRate} onChange={(v) => update("taxRate", Number(v))} /><Field icon={Clock} label="Preparation time (min)" type="number" value={form.preparationTime} onChange={(v) => update("preparationTime", Number(v))} /><Field icon={DollarSign} label="Minimum order ($)" type="number" value={form.minOrder} onChange={(v) => update("minOrder", Number(v))} /><label className="text-sm font-medium text-ink-700">Currency<select value={form.currency} onChange={(e) => update("currency", e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-ink-200 px-3 text-sm"><option>USD</option><option>EUR</option><option>GBP</option><option>CAD</option></select></label></div></Card><Card title="Restaurant information" sub="Business contact and identity" icon={Building2} blue><div className="grid gap-4 sm:grid-cols-2"><div className="sm:col-span-2"><Field icon={Store} label="Restaurant name" value={form.restaurantName} onChange={(v) => update("restaurantName", v)} /></div><Field icon={Mail} label="Email" value={form.restaurantEmail} onChange={(v) => update("restaurantEmail", v)} /><Field icon={Phone} label="Phone" value={form.restaurantPhone} onChange={(v) => update("restaurantPhone", v)} /><Field icon={Globe} label="Website" value={form.restaurantWebsite} onChange={(v) => update("restaurantWebsite", v)} /><div className="sm:col-span-2"><Field icon={MapPin} label="Address" value={form.restaurantAddress} onChange={(v) => update("restaurantAddress", v)} /></div></div></Card></div><section className="rounded-xl border border-ink-200 bg-white p-5 shadow-xs"><h2 className="font-semibold text-ink-900">Customer-facing summary</h2><div className="mt-4 flex flex-wrap gap-x-7 gap-y-3 text-sm"><Summary label="Delivery fee" value={`$${form.deliveryFee.toFixed(2)}`} /><Summary label="Tax rate" value={`${form.taxRate}%`} /><Summary label="Prep time" value={`${form.preparationTime} min`} /><Summary label="Minimum order" value={`$${form.minOrder.toFixed(2)}`} /><Summary label="Currency" value={form.currency} /></div></section>{saved && <div className="fixed bottom-5 right-5 rounded-lg bg-ink-900 px-4 py-3 text-sm text-white">Settings saved successfully.</div>}</div>; }
function Card({ title, sub, icon: Icon, blue, children }) { return <section className="rounded-xl border border-ink-200 bg-white p-5 shadow-xs"><div className="mb-5 flex items-center gap-3"><span className={`grid h-9 w-9 place-items-center rounded-lg ${blue ? "bg-info-50 text-info-600" : "bg-brand-50 text-brand-600"}`}><Icon size={19} /></span><div><h2 className="font-semibold text-ink-900">{title}</h2><p className="text-sm text-ink-500">{sub}</p></div></div>{children}</section>; }
function Field({ label, value, onChange, icon: Icon, type = "text" }) { return <label className="text-sm font-medium text-ink-700">{label}<span className="relative mt-1.5 block"><Icon size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-ink-400" /><input type={type} value={value} onChange={(e) => onChange(e.target.value)} className="h-10 w-full rounded-lg border border-ink-200 py-2 pl-9 pr-3 text-sm text-ink-800 outline-none focus:border-brand-300" /></span></label>; }
function Summary({ label, value }) { return <p className="text-ink-500">{label}: <b className="text-ink-900">{value}</b></p>; }
